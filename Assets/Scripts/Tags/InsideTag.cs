using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InsideTag : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Level2.AddTag("Inside");
        tag = "Inside";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
